# Module Checklist

- `src/<Module>/` skeleton created with hexagonal layout
- `config/modules/<module>.yaml` created
- Doctrine mapping added under the correct entity manager
- At least one command and one query flow scaffolded
- API Platform classes added when the module is API-facing
- `MODULE.md` created
- Unit and functional or integration tests added
