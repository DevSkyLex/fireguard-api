<!DOCTYPE html>
<html lang="en">
  <body>
    <h1>{{ subject }}</h1>
    <p>You have been invited to join <strong>{{ organizationName }}</strong>.</p>
    <p>Use this token to accept your invitation: <code>{{ token }}</code></p>
    <p>This invitation expires at {{ expiresAt }}.</p>
  </body>
</html>
